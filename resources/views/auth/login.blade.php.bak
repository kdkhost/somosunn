@extends('layouts.app')

@section('title', 'Entrar - UNN')

@section('content')
<div class="min-h-screen flex items-center justify-center bg-slate-50 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl w-full bg-white rounded-2xl shadow-lg overflow-hidden grid grid-cols-1 md:grid-cols-2">
        <!-- Left column: promo / imagem -->
        <div class="hidden md:flex flex-col items-center justify-center gap-6 p-10 bg-gradient-to-br from-purple-600 to-blue-600 text-white">
            <img src="{{ asset('img/logo.svg') }}" class="h-20 mb-4" alt="UNN">
            <h2 class="text-2xl font-bold">Bem-vindo de volta!</h2>
            <p class="max-w-xs text-sm">Acesse seu painel, cursos, palestras e mentorias. Entre com suas credenciais ou use login social.</p>
            <div class="flex gap-3 mt-4">
                <a href="{{ route('social.redirect','google') }}" class="bg-white/10 px-4 py-2 rounded">Google</a>
                <a href="{{ route('social.redirect','facebook') }}" class="bg-white/10 px-4 py-2 rounded">Facebook</a>
                <a href="{{ route('social.redirect','linkedin') }}" class="bg-white/10 px-4 py-2 rounded">LinkedIn</a>
            </div>
        </div>

        <!-- Right column: form -->
        <div class="p-8">
            <h3 class="text-2xl font-bold mb-6">{{ __('messages.auth.login') }}</h3>
            <form method="POST" action="{{ route('login') }}" id="loginForm">
                @csrf
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700">{{ __('messages.auth.email') }}</label>
                    <input name="email" type="email" required class="mt-1 w-full rounded-md border-gray-200 p-3" />
                </div>
                <div class="mb-2">
                    <label class="block text-sm font-medium text-gray-700">{{ __('messages.auth.password') }}</label>
                    <input name="password" type="password" required class="mt-1 w-full rounded-md border-gray-200 p-3" />
                </div>
                <div class="flex items-center justify-between text-sm text-gray-500 mb-6">
                    <label><input type="checkbox" /> Lembrar-me</label>
                    <a href="{{ route('password.request') }}">Esqueci minha senha</a>
                </div>
                <button type="submit" class="w-full btn-primary text-white py-3 rounded-lg font-semibold">Entrar</button>
            </form>

            <p class="mt-6 text-center text-sm">Não tem conta? <a href="{{ route('register') }}" class="text-purple-600">Crie uma</a></p>
        </div>
    </div>
</div>

@push('scripts')
<script>
    // Placeholder: add input masks and live validation (password strength, CEP autofill)
    document.addEventListener('DOMContentLoaded', function(){
        // e.g., Inputmask for phone/cep, zxcvbn for password strength
    });
</script>
@endpush

@endsection
